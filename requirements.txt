# Core dependencies for Translation Agent Pro Desktop
openai>=1.28.1
tiktoken>=0.6.0
python-dotenv>=1.0.1
langchain-text-splitters>=0.0.1
icecream>=2.1.3

# GUI and file processing dependencies
python-docx>=1.1.2
PyMuPDF>=1.24.7
simplemma>=1.0.0

# Optional: Gradio for web interface (not needed for desktop)
# gradio>=4.37.2

# Note: python-docx is required for DOCX output format
# If not installed, the application will fallback to TXT format