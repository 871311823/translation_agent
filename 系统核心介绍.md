# Translation Agent 系统核心介绍

## 📋 系统概述

**Translation Agent** 是一个基于大语言模型（LLM）的智能翻译系统，采用**反思式工作流（Reflection Workflow）**来提升翻译质量。与传统机器翻译系统不同，它通过"翻译→反思→改进"的三阶段流程，让LLM自己评估和改进翻译结果。

### 核心创新点

1. **反思式工作流**：不是一次性翻译，而是让LLM先翻译，再反思，最后改进
2. **高度可定制**：通过提示词可以控制风格、术语、地域变体等
3. **上下文感知**：长文本分块处理时保持上下文连贯性
4. **多LLM支持**：支持OpenAI、Groq、TogetherAI、Ollama等

---

## 🎯 核心工作流程

### 三阶段翻译流程

```
源文本
  ↓
[阶段1] 初始翻译 (Initial Translation)
  ↓
初始翻译结果
  ↓
[阶段2] 反思评估 (Reflection)
  ↓
改进建议
  ↓
[阶段3] 改进翻译 (Improved Translation)
  ↓
最终翻译结果
```

---

## 💎 精华代码

### 1. 核心翻译函数

```python
def one_chunk_translate_text(
    source_lang: str, 
    target_lang: str, 
    source_text: str, 
    country: str = ""
) -> str:
    """三阶段翻译流程的核心实现"""
    
    # 阶段1: 初始翻译
    translation_1 = one_chunk_initial_translation(
        source_lang, target_lang, source_text
    )
    
    # 阶段2: 反思评估
    reflection = one_chunk_reflect_on_translation(
        source_lang, target_lang, source_text, translation_1, country
    )
    
    # 阶段3: 改进翻译
    translation_2 = one_chunk_improve_translation(
        source_lang, target_lang, source_text, translation_1, reflection
    )
    
    return translation_2
```

### 2. 长文本分块处理

```python
def translate(
    source_lang, target_lang, source_text, country,
    max_tokens=MAX_TOKENS_PER_CHUNK
):
    """智能分块处理长文本"""
    
    num_tokens = num_tokens_in_string(source_text)
    
    if num_tokens < max_tokens:
        # 短文本：单块处理
        return one_chunk_translate_text(...)
    else:
        # 长文本：分块处理，保持上下文
        text_splitter = RecursiveCharacterTextSplitter(...)
        chunks = text_splitter.split_text(source_text)
        return multichunk_translation(...)
```

---

## 🔑 核心提示词（Prompts）

### 1. 初始翻译提示词

**System Message:**
```
You are an expert linguist, specializing in translation from {source_lang} to {target_lang}.
```

**User Prompt:**
```
This is an {source_lang} to {target_lang} translation, please provide the {target_lang} translation for this text.
Do not provide any explanations or text apart from the translation.

{source_lang}: {source_text}

{target_lang}:
```

**关键特点：**
- 简洁直接，要求只输出翻译
- 不要求解释，避免冗余输出

---

### 2. 反思评估提示词（核心）

**System Message:**
```
You are an expert linguist specializing in translation from {source_lang} to {target_lang}.
You will be provided with a source text and its translation and your goal is to improve the translation.
```

**User Prompt（带地区变体）:**
```
Your task is to carefully read a source text and a translation from {source_lang} to {target_lang}, 
and then give constructive criticism and helpful suggestions to improve the translation.
The final style and tone of the translation should match the style of {target_lang} 
colloquially spoken in {country}.

The source text and initial translation, delimited by XML tags 
<SOURCE_TEXT></SOURCE_TEXT> and <TRANSLATION></TRANSLATION>, are as follows:

<SOURCE_TEXT>
{source_text}
</SOURCE_TEXT>

<TRANSLATION>
{translation_1}
</TRANSLATION>

When writing suggestions, pay attention to whether there are ways to improve the translation's:
(i) accuracy (by correcting errors of addition, mistranslation, omission, or untranslated text),
(ii) fluency (by applying {target_lang} grammar, spelling and punctuation rules, 
     and ensuring there are no unnecessary repetitions),
(iii) style (by ensuring the translations reflect the style of the source text 
      and take into account any cultural context),
(iv) terminology (by ensuring terminology use is consistent and reflects the source text domain; 
     and by only ensuring you use equivalent idioms {target_lang}).

Write a list of specific, helpful and constructive suggestions for improving the translation.
Each suggestion should address one specific part of the translation.
Output only the suggestions and nothing else.
```

**关键特点：**
- 使用XML标签结构化输入
- 明确四个评估维度：准确性、流畅性、风格、术语
- 支持地区变体（如"西班牙语在墨西哥的使用"）
- 要求具体、可操作的改进建议

---

### 3. 改进翻译提示词

**System Message:**
```
You are an expert linguist, specializing in translation editing from {source_lang} to {target_lang}.
```

**User Prompt:**
```
Your task is to carefully read, then edit, a translation from {source_lang} to {target_lang}, 
taking into account a list of expert suggestions and constructive criticisms.

The source text, the initial translation, and the expert linguist suggestions are delimited by 
XML tags <SOURCE_TEXT></SOURCE_TEXT>, <TRANSLATION></TRANSLATION> and 
<EXPERT_SUGGESTIONS></EXPERT_SUGGESTIONS> as follows:

<SOURCE_TEXT>
{source_text}
</SOURCE_TEXT>

<TRANSLATION>
{translation_1}
</TRANSLATION>

<EXPERT_SUGGESTIONS>
{reflection}
</EXPERT_SUGGESTIONS>

Please take into account the expert suggestions when editing the translation. 
Edit the translation by ensuring:

(i) accuracy (by correcting errors of addition, mistranslation, omission, or untranslated text),
(ii) fluency (by applying {target_lang} grammar, spelling and punctuation rules 
     and ensuring there are no unnecessary repetitions),
(iii) style (by ensuring the translations reflect the style of the source text)
(iv) terminology (inappropriate for context, inconsistent use), or
(v) other errors.

Output only the new translation and nothing else.
```

**关键特点：**
- 整合三个输入：源文本、初始翻译、反思建议
- 明确改进方向
- 只输出最终翻译，无其他内容

---

## 🏗️ 系统架构

### 文件结构

```
translation-agent/
├── src/translation_agent/
│   ├── utils.py          # 核心翻译逻辑（三阶段工作流）
│   └── __init__.py
├── app/
│   ├── app.py            # Gradio WebUI界面
│   ├── process.py        # 处理逻辑（集成utils）
│   └── patch.py          # LLM端点配置和速率限制
└── examples/
    └── example_script.py # 使用示例
```

### 核心模块

1. **utils.py** - 翻译核心引擎
   - `one_chunk_initial_translation()` - 单块初始翻译
   - `one_chunk_reflect_on_translation()` - 单块反思评估
   - `one_chunk_improve_translation()` - 单块改进翻译
   - `multichunk_*()` - 多块版本（保持上下文）

2. **process.py** - 业务逻辑层
   - `translator()` - 主翻译函数
   - `translator_sec()` - 使用额外端点进行反思

3. **app.py** - 用户界面层
   - Gradio WebUI
   - 配置管理
   - 文件上传处理

---

## 🎨 核心设计理念

### 1. 反思式工作流（Reflection Workflow）

**为什么有效？**
- LLM在第一次翻译时可能遗漏细节
- 通过"自我审视"，可以发现并纠正错误
- 类似于人类翻译的"初译→审校→定稿"流程

### 2. 结构化提示词

**XML标签的使用：**
- `<SOURCE_TEXT>` - 源文本
- `<TRANSLATION>` - 翻译结果
- `<EXPERT_SUGGESTIONS>` - 改进建议
- `<TRANSLATE_THIS>` - 分块翻译时的标记

**优势：**
- 清晰的结构化输入
- 避免混淆不同部分
- 提高LLM理解准确性

### 3. 四维度评估体系

1. **准确性（Accuracy）**
   - 纠正增译、误译、漏译
   - 确保无未翻译文本

2. **流畅性（Fluency）**
   - 语法正确性
   - 拼写和标点
   - 避免重复

3. **风格（Style）**
   - 匹配源文本风格
   - 考虑文化背景
   - 正式/非正式语调

4. **术语（Terminology）**
   - 术语一致性
   - 领域特定术语
   - 习语等效翻译

---

## 🚀 关键技术特性

### 1. 智能分块处理

```python
# 自动计算分块大小
token_size = calculate_chunk_size(
    token_count=num_tokens_in_text, 
    token_limit=max_tokens
)

# 使用上下文感知的分块器
text_splitter = RecursiveCharacterTextSplitter.from_tiktoken_encoder(
    model_name="gpt-4",
    chunk_size=token_size,
    chunk_overlap=0,  # 可以设置重叠以保持上下文
)
```

### 2. 上下文保持

在分块翻译时，每个块都能看到：
- 前面的源文本（作为上下文）
- 当前要翻译的块（标记为`<TRANSLATE_THIS>`）
- 后面的源文本（作为上下文）

### 3. 速率限制

```python
@rate_limit(lambda: RPM)
def get_completion(...):
    # 自动控制API调用频率
    # 避免超过速率限制
```

---

## 📊 使用示例

### Python API使用

```python
import translation_agent as ta

translation = ta.translate(
    source_lang="Chinese",
    target_lang="English",
    source_text="你好，世界！",
    country="United States"
)
```

### WebUI使用

1. 访问 `http://47.109.82.94:7860`
2. 选择API端点（OpenAI/Groq/CUSTOM等）
3. 输入源文本
4. 点击"翻译"按钮
5. 查看：初始翻译 → 反思建议 → 最终翻译

---

## 🔍 提示词设计要点

### 1. 角色定位明确

```
"You are an expert linguist, specializing in translation..."
```

- 明确LLM的角色
- 设定专业领域
- 建立专业权威性

### 2. 任务描述清晰

```
"Your task is to carefully read... and then give constructive criticism..."
```

- 明确任务目标
- 说明操作步骤
- 设定输出格式

### 3. 结构化输入

使用XML标签分隔不同部分：
- 避免混淆
- 提高解析准确性
- 便于LLM理解结构

### 4. 评估维度明确

```
(i) accuracy
(ii) fluency  
(iii) style
(iv) terminology
```

- 系统化的评估框架
- 可操作的改进方向
- 全面的质量保证

### 5. 输出格式控制

```
"Output only the suggestions and nothing else."
"Output only the new translation and nothing else."
```

- 严格控制输出格式
- 避免冗余信息
- 便于后续处理

---

## 💡 系统优势

1. **质量提升**：通过反思机制，翻译质量通常优于单次翻译
2. **高度可定制**：通过提示词可以控制风格、术语、地域变体
3. **上下文感知**：长文本处理时保持上下文连贯性
4. **灵活扩展**：易于添加新的LLM提供商或自定义端点
5. **用户友好**：提供WebUI界面，支持文件上传

---

## 🎯 适用场景

- **专业文档翻译**：需要高质量、术语准确的翻译
- **多语言内容创作**：需要特定风格和地域变体
- **批量翻译处理**：支持长文本和文件上传
- **翻译质量评估**：可以看到反思过程，了解改进点

---

## 📝 核心代码位置

- **翻译核心逻辑**：`src/translation_agent/utils.py`
- **提示词定义**：`src/translation_agent/utils.py` (第87-228行)
- **WebUI界面**：`app/app.py`
- **业务处理**：`app/process.py`

---

## 🔬 技术细节

### Token计算

```python
def num_tokens_in_string(input_str: str, encoding_name: str = "cl100k_base") -> int:
    """使用tiktoken计算token数量"""
    encoding = tiktoken.get_encoding(encoding_name)
    return len(encoding.encode(input_str))
```

### 分块大小计算

```python
def calculate_chunk_size(token_count: int, token_limit: int) -> int:
    """智能计算分块大小，确保均匀分布"""
    if token_count <= token_limit:
        return token_count
    
    num_chunks = (token_count + token_limit - 1) // token_limit
    chunk_size = token_count // num_chunks
    
    remaining_tokens = token_count % token_limit
    if remaining_tokens > 0:
        chunk_size += remaining_tokens // num_chunks
    
    return chunk_size
```

---

## 🌟 创新亮点

1. **自我改进机制**：LLM自己评估和改进翻译
2. **结构化提示词**：使用XML标签提高准确性
3. **四维度评估**：系统化的质量评估框架
4. **上下文保持**：长文本分块时保持连贯性
5. **高度可定制**：通过提示词控制各种翻译参数

---

## 📚 相关研究

- *ChatGPT MT: Competitive for High- (but not Low-) Resource Languages*, Robinson et al. (2023)
- *How to Design Translation Prompts for ChatGPT: An Empirical Study*, Gao et al. (2023)
- *Beyond Human Translation: Harnessing Multi-Agent Collaboration for Translating Ultra-Long Literary Texts*, Wu et al. (2024)

---

## 🎓 总结

Translation Agent 通过**反思式工作流**和**结构化提示词**，实现了高质量的智能翻译。核心思想是让LLM"先翻译，再反思，最后改进"，类似于人类翻译的专业流程。通过明确的评估维度和清晰的提示词设计，系统能够产生比单次翻译更准确、更流畅、更符合要求的翻译结果。


